# Phase 0 Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the documentation-only HouseKeep repository into a runnable Flutter project with the agreed architecture, theme foundation, i18n, drift setup, navigation shell, and Firebase initialization path.

**Architecture:** Implement Phase 0 in vertical slices that leave the app runnable after each major checkpoint. Start with project scaffolding and tooling, then add theme and shell, then localization, then database setup, then Firebase, and finish with verification and checklist updates.

**Tech Stack:** Flutter 3.x, Material 3, flutter_riverpod, riverpod_generator, go_router, drift, sqlite3_flutter_libs, Firebase Core/Analytics/Crashlytics, Flutter gen-l10n, build_runner.

---

## File Structure Map

### Existing files to keep

- `README.md` — project summary and phase status.
- `docs/ARCHITECTURE.md` — source of truth for folders, dependencies, and theme constraints.
- `docs/DATA_MODEL.md` — source of truth for drift schema field names.
- `docs/PHASE_CHECKLIST.md` — must be updated with real completion state at the end.
- `docs/superpowers/specs/2026-05-20-phase-0-setup-design.md` — approved design spec for this phase.

### Generated project files

- Create: `pubspec.yaml` — Flutter dependencies and assets.
- Create: `analysis_options.yaml` — linting, including `riverpod_lint`.
- Create: `.gitignore` — Flutter and Dart ignores.
- Create: platform folders generated by Flutter (`android/`, `ios/`, `linux/`, `macos/`, `web/`, `windows/`).

### App entry and shell

- Create: `lib/main.dart` — app bootstrap, later extended with Firebase initialization.
- Create: `lib/app.dart` — `MaterialApp.router`, localization delegates, and `go_router` shell.
- Create: `lib/features/home/home_screen.dart` — initial Home tab.
- Create: `lib/features/items/items_list_screen.dart` — initial Items tab.
- Create: `lib/features/documents/documents_list_screen.dart` — initial Documents tab.
- Create: `lib/features/settings/settings_screen.dart` — initial Settings tab.
- Create: `test/app_smoke_test.dart` — minimal shell smoke test.

### Theme and constants

- Create: `lib/core/theme/app_colors.dart` — semantic palette from the architecture doc.
- Create: `lib/core/theme/app_typography.dart` — shared text styles using platform-safe fonts.
- Create: `lib/core/theme/app_theme.dart` — Material 3 `ThemeData` and `ColorScheme`.
- Create: `lib/core/constants/app_constants.dart` — free limits and small app-wide constants.
- Create: `lib/core/constants/asset_paths.dart` — centralized asset paths.

### Localization

- Create: `l10n.yaml` — localization generation config.
- Create: `lib/core/l10n/app_en.arb` — English strings.
- Create: `lib/core/l10n/app_es.arb` — Spanish strings.

### Database setup

- Create: `lib/data/database/tables/items_table.dart` — Items schema.
- Create: `lib/data/database/tables/maintenances_table.dart` — Maintenances schema.
- Create: `lib/data/database/tables/documents_table.dart` — Documents schema.
- Create: `lib/data/database/app_database.dart` — drift database definition and SQLite connection.
- Create: `lib/data/database/app_database.g.dart` — generated by build_runner.

### Assets and docs

- Create: `assets/templates/.gitkeep`
- Create: `assets/images/.gitkeep`
- Modify: `docs/PHASE_CHECKLIST.md` — mark only completed Phase 0 items.
- Modify: `README.md` — update status only if Phase 0 is fully complete.

---

### Task 1: Scaffold Flutter Project And Tooling

**Files:**
- Create: `/home/darumo/Proyectos/housekeep` Flutter scaffold output
- Modify: `/home/darumo/Proyectos/housekeep/pubspec.yaml`
- Modify: `/home/darumo/Proyectos/housekeep/analysis_options.yaml`
- Create: `/home/darumo/Proyectos/housekeep/l10n.yaml`

- [ ] **Step 1: Verify Flutter tooling before scaffolding**

Run:

```bash
flutter --version
flutter doctor
```

Expected:
- `flutter --version` prints a stable Flutter 3.x version.
- `flutter doctor` reports no Android-blocking issues.
- On Linux, iOS/Xcode can remain unavailable and must not block the rest of the phase.

- [ ] **Step 2: Create the Flutter project in the existing repository root**

Run:

```bash
flutter create --org com.housekeep --project-name housekeep /home/darumo/Proyectos/housekeep
```

Expected:
- Flutter generates the standard project files without deleting `docs/`, `README.md`, or `AGENTS.md`.
- A generated `pubspec.yaml`, `lib/main.dart`, and platform folders appear in the root.

- [ ] **Step 3: Replace `pubspec.yaml` dependencies and asset registration**

Write this content into `pubspec.yaml`:

```yaml
name: housekeep
description: HouseKeep home maintenance app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0
  go_router: ^14.0.0
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.0
  purchases_flutter: ^8.0.0
  firebase_core: ^3.0.0
  firebase_analytics: ^11.0.0
  firebase_crashlytics: ^4.0.0
  intl: ^0.19.0
  uuid: ^4.3.0
  image_picker: ^1.0.0
  shared_preferences: ^2.2.0
  home_widget: ^0.6.0
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  drift_dev: ^2.16.0
  riverpod_generator: ^2.4.0
  custom_lint: ^0.6.0
  riverpod_lint: ^2.3.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/templates/
    - assets/images/
```

- [ ] **Step 4: Configure analysis and localization generation files**

Write this content into `analysis_options.yaml`:

```yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  plugins:
    - custom_lint

linter:
  rules:
    avoid_print: true
```

Write this content into `l10n.yaml`:

```yaml
arb-dir: lib/core/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
output-dir: lib/core/l10n/generated
nullable-getter: false
```

- [ ] **Step 5: Fetch dependencies and initialize git history**

Run:

```bash
flutter pub get
git init
git add pubspec.yaml pubspec.lock analysis_options.yaml l10n.yaml .gitignore README.md docs AGENTS.md
git commit -m "chore: bootstrap flutter project"
```

Expected:
- `flutter pub get` completes successfully.
- `git init` creates a repository in `/home/darumo/Proyectos/housekeep`.
- The first commit records the scaffold and base tooling files.

---

### Task 2: Build Theme Foundation With UI Skill Guidance

**Files:**
- Create: `lib/core/theme/app_colors.dart`
- Create: `lib/core/theme/app_typography.dart`
- Create: `lib/core/theme/app_theme.dart`
- Create: `lib/core/constants/app_constants.dart`
- Create: `lib/core/constants/asset_paths.dart`
- Create: `assets/templates/.gitkeep`
- Create: `assets/images/.gitkeep`

- [ ] **Step 1: Generate visual guidance with `ui-ux-pro-max`**

Run:

```bash
python3 ".opencode/skills/ui-ux-pro-max/scripts/search.py" "household maintenance mobile app warm rounded minimal trustworthy material 3" --design-system -f markdown -p "HouseKeep"
python3 ".opencode/skills/ui-ux-pro-max/scripts/search.py" "warm trustworthy rounded home utility mobile" --domain style -n 5
python3 ".opencode/skills/ui-ux-pro-max/scripts/search.py" "mobile navigation accessibility touch targets" --domain ux -n 8
python3 ".opencode/skills/ui-ux-pro-max/scripts/search.py" "theming navigation accessibility material 3 mobile" --stack flutter
```

Apply the results this way:
- Keep the documented HouseKeep palette from `docs/ARCHITECTURE.md` as the color source of truth.
- Use the skill output for shape, spacing, touch-target, and interaction guidance.
- Prefer the `Organic Biophilic`, `Bento Grids`, and `Micro-interactions` cues over the suggested pink palette.

- [ ] **Step 2: Create `lib/core/constants/app_constants.dart`**

Write:

```dart
class AppConstants {
  const AppConstants._();

  static const int freeItemsLimit = 5;
  static const int freeDocumentsLimit = 3;
  static const String bundleId = 'com.housekeep.app';
  static const String entitlementId = 'housekeep_pro';
}
```

- [ ] **Step 3: Create `lib/core/constants/asset_paths.dart` and asset directories**

Write `lib/core/constants/asset_paths.dart`:

```dart
class AssetPaths {
  const AssetPaths._();

  static const String templatesDir = 'assets/templates';
  static const String imagesDir = 'assets/images';
  static const String maintenanceTemplates = 'assets/templates/maintenance_templates.json';
}
```

Run:

```bash
mkdir -p assets/templates assets/images
touch assets/templates/.gitkeep assets/images/.gitkeep
```

- [ ] **Step 4: Create the theme files**

Write `lib/core/theme/app_colors.dart`:

```dart
import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF2E7D6F);
  static const primaryLight = Color(0xFF4DA89A);
  static const primaryDark = Color(0xFF1B5E50);
  static const onPrimary = Color(0xFFFFFFFF);

  static const secondary = Color(0xFFF5A623);
  static const secondaryLight = Color(0xFFFFBE45);
  static const onSecondary = Color(0xFF1A1A1A);

  static const background = Color(0xFFFAFAF8);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF5F5F3);

  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFF9A825);
  static const error = Color(0xFFD32F2F);

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);
  static const textTertiary = Color(0xFF9E9E9E);
}
```

Write `lib/core/theme/app_typography.dart`:

```dart
import 'package:flutter/material.dart';

class AppTypography {
  const AppTypography._();

  static TextTheme textTheme(Color textColor) {
    return TextTheme(
      headlineLarge: TextStyle(
        fontSize: 30,
        fontWeight: FontWeight.w700,
        color: textColor,
        letterSpacing: -0.4,
      ),
      headlineSmall: TextStyle(
        fontSize: 24,
        fontWeight: FontWeight.w700,
        color: textColor,
      ),
      titleLarge: TextStyle(
        fontSize: 20,
        fontWeight: FontWeight.w600,
        color: textColor,
      ),
      titleMedium: TextStyle(
        fontSize: 16,
        fontWeight: FontWeight.w600,
        color: textColor,
      ),
      bodyLarge: TextStyle(
        fontSize: 16,
        fontWeight: FontWeight.w400,
        color: textColor,
        height: 1.4,
      ),
      bodyMedium: TextStyle(
        fontSize: 14,
        fontWeight: FontWeight.w400,
        color: textColor,
        height: 1.4,
      ),
      labelLarge: TextStyle(
        fontSize: 14,
        fontWeight: FontWeight.w600,
        color: textColor,
      ),
    );
  }
}
```

Write `lib/core/theme/app_theme.dart`:

```dart
import 'package:flutter/material.dart';

import 'app_colors.dart';
import 'app_typography.dart';

class AppTheme {
  const AppTheme._();

  static ThemeData light() {
    final colorScheme = ColorScheme.fromSeed(
      seedColor: AppColors.primary,
      brightness: Brightness.light,
      surface: AppColors.surface,
    ).copyWith(
      primary: AppColors.primary,
      onPrimary: AppColors.onPrimary,
      secondary: AppColors.secondary,
      onSecondary: AppColors.onSecondary,
      error: AppColors.error,
      onSurface: AppColors.textPrimary,
    );

    final base = ThemeData(
      useMaterial3: true,
      colorScheme: colorScheme,
      scaffoldBackgroundColor: AppColors.background,
      textTheme: AppTypography.textTheme(AppColors.textPrimary),
    );

    return base.copyWith(
      cardTheme: const CardThemeData(
        elevation: 1,
        color: AppColors.surface,
        margin: EdgeInsets.zero,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.all(Radius.circular(20)),
        ),
      ),
      inputDecorationTheme: InputDecorationTheme(
        filled: true,
        fillColor: AppColors.surfaceVariant,
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(16),
          borderSide: BorderSide.none,
        ),
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
      ),
      navigationBarTheme: NavigationBarThemeData(
        backgroundColor: AppColors.surface,
        elevation: 0,
        height: 72,
        labelTextStyle: WidgetStateProperty.all(
          const TextStyle(fontWeight: FontWeight.w600),
        ),
        indicatorColor: AppColors.primary.withOpacity(0.12),
      ),
    );
  }
}
```

- [ ] **Step 5: Verify formatting, analysis, and commit the theme foundation**

Run:

```bash
dart format lib
flutter analyze
git add lib/core assets pubspec.yaml
git commit -m "feat: add app theme foundation"
```

Expected:
- `flutter analyze` passes or reports only issues you fix before committing.
- Theme files compile cleanly before app-shell wiring starts.

---

### Task 3: Wire App Shell, Localization, And Initial Screens

**Files:**
- Create: `lib/main.dart`
- Create: `lib/app.dart`
- Create: `lib/core/l10n/app_en.arb`
- Create: `lib/core/l10n/app_es.arb`
- Create: `lib/features/home/home_screen.dart`
- Create: `lib/features/items/items_list_screen.dart`
- Create: `lib/features/documents/documents_list_screen.dart`
- Create: `lib/features/settings/settings_screen.dart`
- Create: `test/app_smoke_test.dart`

- [ ] **Step 1: Add localized strings for the base shell**

Write `lib/core/l10n/app_en.arb`:

```json
{
  "@@locale": "en",
  "appTitle": "HouseKeep",
  "homeTab": "Home",
  "itemsTab": "Items",
  "documentsTab": "Documents",
  "settingsTab": "Settings",
  "homeTitle": "Your home at a glance",
  "itemsTitle": "Items",
  "documentsTitle": "Documents",
  "settingsTitle": "Settings",
  "phaseZeroMessage": "Phase 0 foundation is ready for the next feature phase."
}
```

Write `lib/core/l10n/app_es.arb`:

```json
{
  "@@locale": "es",
  "appTitle": "HouseKeep",
  "homeTab": "Inicio",
  "itemsTab": "Items",
  "documentsTab": "Documentos",
  "settingsTab": "Ajustes",
  "homeTitle": "Tu casa de un vistazo",
  "itemsTitle": "Items",
  "documentsTitle": "Documentos",
  "settingsTitle": "Ajustes",
  "phaseZeroMessage": "La base de la Fase 0 ya está lista para la siguiente fase."
}
```

- [ ] **Step 2: Create the four initial screens**

Write `lib/features/home/home_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../../core/l10n/generated/app_localizations.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);

    return Padding(
      padding: const EdgeInsets.all(20),
      child: Card(
        child: Padding(
          padding: const EdgeInsets.all(20),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            mainAxisSize: MainAxisSize.min,
            children: [
              Text(l10n.homeTitle, style: Theme.of(context).textTheme.titleLarge),
              const SizedBox(height: 12),
              Text(l10n.phaseZeroMessage),
            ],
          ),
        ),
      ),
    );
  }
}
```

Write `lib/features/items/items_list_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../../core/l10n/generated/app_localizations.dart';

class ItemsListScreen extends StatelessWidget {
  const ItemsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);

    return Center(child: Text(l10n.phaseZeroMessage));
  }
}
```

Write `lib/features/documents/documents_list_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../../core/l10n/generated/app_localizations.dart';

class DocumentsListScreen extends StatelessWidget {
  const DocumentsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);

    return Center(child: Text(l10n.phaseZeroMessage));
  }
}
```

Write `lib/features/settings/settings_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../../core/l10n/generated/app_localizations.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);

    return Center(child: Text(l10n.phaseZeroMessage));
  }
}
```

- [ ] **Step 3: Create `lib/app.dart` with router and bottom navigation**

Write:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:go_router/go_router.dart';

import 'core/l10n/generated/app_localizations.dart';
import 'core/theme/app_theme.dart';
import 'features/documents/documents_list_screen.dart';
import 'features/home/home_screen.dart';
import 'features/items/items_list_screen.dart';
import 'features/settings/settings_screen.dart';

class HouseKeepApp extends StatelessWidget {
  HouseKeepApp({super.key});

  final GoRouter _router = GoRouter(
    routes: [
      ShellRoute(
        builder: (context, state, child) {
          return _AppShell(location: state.uri.toString(), child: child);
        },
        routes: [
          GoRoute(path: '/', builder: (context, state) => const HomeScreen()),
          GoRoute(path: '/items', builder: (context, state) => const ItemsListScreen()),
          GoRoute(path: '/documents', builder: (context, state) => const DocumentsListScreen()),
          GoRoute(path: '/settings', builder: (context, state) => const SettingsScreen()),
        ],
      ),
    ],
  );

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      onGenerateTitle: (context) => AppLocalizations.of(context).appTitle,
      theme: AppTheme.light(),
      routerConfig: _router,
      supportedLocales: AppLocalizations.supportedLocales,
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
    );
  }
}

class _AppShell extends StatelessWidget {
  const _AppShell({required this.location, required this.child});

  final String location;
  final Widget child;

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context);
    final destinations = <String>['/', '/items', '/documents', '/settings'];
    final currentIndex = switch (location) {
      '/items' => 1,
      '/documents' => 2,
      '/settings' => 3,
      _ => 0,
    };
    final title = switch (currentIndex) {
      1 => l10n.itemsTitle,
      2 => l10n.documentsTitle,
      3 => l10n.settingsTitle,
      _ => l10n.homeTab,
    };

    return Scaffold(
      appBar: AppBar(title: Text(title)),
      body: SafeArea(child: child),
      bottomNavigationBar: NavigationBar(
        selectedIndex: currentIndex,
        onDestinationSelected: (index) => context.go(destinations[index]),
        destinations: [
          NavigationDestination(icon: const Icon(Icons.home_outlined), selectedIcon: const Icon(Icons.home), label: l10n.homeTab),
          NavigationDestination(icon: const Icon(Icons.kitchen_outlined), selectedIcon: const Icon(Icons.kitchen), label: l10n.itemsTab),
          NavigationDestination(icon: const Icon(Icons.description_outlined), selectedIcon: const Icon(Icons.description), label: l10n.documentsTab),
          NavigationDestination(icon: const Icon(Icons.settings_outlined), selectedIcon: const Icon(Icons.settings), label: l10n.settingsTab),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: Create `lib/main.dart`, add a smoke test, and generate localizations**

Write `lib/main.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'app.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const ProviderScope(child: _AppBootstrap()));
}

class _AppBootstrap extends StatelessWidget {
  const _AppBootstrap();

  @override
  Widget build(BuildContext context) {
    return HouseKeepApp();
  }
}
```

Write `test/app_smoke_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';

import 'package:housekeep/app.dart';

void main() {
  testWidgets('shows the home tab shell', (tester) async {
    await tester.pumpWidget(HouseKeepApp());
    await tester.pumpAndSettle();

    expect(find.text('Your home at a glance'), findsOneWidget);
  });
}
```

Run:

```bash
flutter gen-l10n
dart format lib
flutter analyze
flutter test test/app_smoke_test.dart
```

Expected:
- `lib/core/l10n/generated/app_localizations.dart` is generated.
- Analyzer passes with the new imports and generated l10n file.
- The smoke test passes.

- [ ] **Step 5: Commit the shell and localization baseline**

Run:

```bash
git add lib/main.dart lib/app.dart lib/core/l10n lib/features/home lib/features/items lib/features/documents lib/features/settings test/app_smoke_test.dart
git commit -m "feat: add localized app shell"
```

---

### Task 4: Configure Drift Database And Code Generation

**Files:**
- Create: `lib/data/database/tables/items_table.dart`
- Create: `lib/data/database/tables/maintenances_table.dart`
- Create: `lib/data/database/tables/documents_table.dart`
- Create: `lib/data/database/app_database.dart`

- [ ] **Step 1: Create the three table files using the documented schema**

Write `lib/data/database/tables/items_table.dart`:

```dart
import 'package:drift/drift.dart';

class ItemsTable extends Table {
  TextColumn get id => text()();
  TextColumn get name => text()();
  TextColumn get category => text()();
  TextColumn get brand => text().nullable()();
  TextColumn get model => text().nullable()();
  IntColumn get purchaseDate => integer().named('purchase_date').nullable()();
  IntColumn get warrantyMonths => integer().named('warranty_months').nullable()();
  TextColumn get photoPath => text().named('photo_path').nullable()();
  TextColumn get notes => text().nullable()();
  IntColumn get createdAt => integer().named('created_at')();
  IntColumn get updatedAt => integer().named('updated_at')();

  @override
  Set<Column<Object>> get primaryKey => {id};

  @override
  List<Set<Column<Object>>> get uniqueKeys => [
        {id},
      ];
}
```

Write `lib/data/database/tables/maintenances_table.dart`:

```dart
import 'package:drift/drift.dart';

import 'items_table.dart';

class MaintenancesTable extends Table {
  TextColumn get id => text()();
  TextColumn get itemId => text().named('item_id').references(ItemsTable, #id)();
  TextColumn get name => text()();
  TextColumn get description => text().nullable()();
  IntColumn get intervalMonths => integer().named('interval_months')();
  IntColumn get lastDoneAt => integer().named('last_done_at').nullable()();
  IntColumn get nextDueAt => integer().named('next_due_at')();
  IntColumn get notifyDaysBefore => integer().named('notify_days_before').withDefault(const Constant(7))();
  BoolColumn get isFromTemplate => boolean().named('is_from_template').withDefault(const Constant(false))();
  IntColumn get createdAt => integer().named('created_at')();
  IntColumn get updatedAt => integer().named('updated_at')();

  @override
  Set<Column<Object>> get primaryKey => {id};
}
```

Write `lib/data/database/tables/documents_table.dart`:

```dart
import 'package:drift/drift.dart';

class DocumentsTable extends Table {
  TextColumn get id => text()();
  TextColumn get name => text()();
  TextColumn get type => text()();
  IntColumn get expiryDate => integer().named('expiry_date')();
  IntColumn get notifyDaysBefore => integer().named('notify_days_before').withDefault(const Constant(30))();
  TextColumn get photoPath => text().named('photo_path').nullable()();
  TextColumn get notes => text().nullable()();
  IntColumn get createdAt => integer().named('created_at')();
  IntColumn get updatedAt => integer().named('updated_at')();

  @override
  Set<Column<Object>> get primaryKey => {id};
}
```

- [ ] **Step 2: Create the drift database entry point**

Write `lib/data/database/app_database.dart`:

```dart
import 'dart:io';

import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:path/path.dart' as p;
import 'package:path_provider/path_provider.dart';

import 'tables/documents_table.dart';
import 'tables/items_table.dart';
import 'tables/maintenances_table.dart';

part 'app_database.g.dart';

@DriftDatabase(tables: [ItemsTable, MaintenancesTable, DocumentsTable])
class AppDatabase extends _$AppDatabase {
  AppDatabase() : super(_openConnection());

  @override
  int get schemaVersion => 1;
}

LazyDatabase _openConnection() {
  return LazyDatabase(() async {
    final directory = await getApplicationDocumentsDirectory();
    final file = File(p.join(directory.path, 'housekeep.sqlite'));

    return NativeDatabase.createInBackground(file);
  });
}
```

- [ ] **Step 3: Generate drift code and fix any schema issues immediately**

Run:

```bash
dart run build_runner build --delete-conflicting-outputs
flutter analyze
```

Expected:
- `lib/data/database/app_database.g.dart` is generated.
- Analyzer passes with no unresolved drift symbols.

- [ ] **Step 4: Keep the app connected to the database entry point without adding repositories yet**

Add this import-only smoke usage to `lib/main.dart` so the database file is compiled by the analyzer:

```dart
import 'data/database/app_database.dart';
```

Then instantiate and immediately dispose the database during startup validation by replacing `main()` with:

```dart
void main() {
  WidgetsFlutterBinding.ensureInitialized();
  final database = AppDatabase();
  database.close();
  runApp(const ProviderScope(child: _AppBootstrap()));
}
```

This keeps Phase 0 minimal while ensuring the generated drift setup is actually referenced.

- [ ] **Step 5: Commit the database setup**

Run:

```bash
git add lib/data/database lib/main.dart
git commit -m "feat: add drift database setup"
```

---

### Task 5: Integrate Firebase Bootstrap

**Files:**
- Modify: `android/app/build.gradle` or `android/app/build.gradle.kts`
- Modify: `android/build.gradle` or `android/build.gradle.kts`
- Create: `android/app/google-services.json`
- Create: `ios/Runner/GoogleService-Info.plist`
- Create: `lib/firebase_options.dart`
- Modify: `lib/main.dart`

- [ ] **Step 1: Install FlutterFire CLI and create the Firebase project**

Run:

```bash
dart pub global activate flutterfire_cli
firebase login
```

In Firebase Console:
- Create project display name `HouseKeep`.
- Use project id `housekeep-app` if available.
- Disable advanced Analytics extras during creation.
- Register Android package `com.housekeep.app`.
- Register iOS bundle `com.housekeep.app`.

- [ ] **Step 2: Add platform config files and generate FlutterFire options**

Place the downloaded files here:

```text
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
```

Run:

```bash
flutterfire configure --project=housekeep-app --platforms=android,ios
```

Expected:
- `lib/firebase_options.dart` is generated.
- Android and iOS firebase settings are patched by FlutterFire.

- [ ] **Step 3: Replace `lib/main.dart` with Firebase-aware bootstrap**

Write:

```dart
import 'package:firebase_core/firebase_core.dart';
import 'package:firebase_crashlytics/firebase_crashlytics.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'app.dart';
import 'firebase_options.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();

  try {
    await Firebase.initializeApp(
      options: DefaultFirebaseOptions.currentPlatform,
    );

    FlutterError.onError = FirebaseCrashlytics.instance.recordFlutterFatalError;
  } on UnsupportedError {
    // Linux desktop can stay unconfigured in Phase 0 while Android/iOS remain configured.
  }

  runApp(const ProviderScope(child: _AppBootstrap()));
}

class _AppBootstrap extends StatelessWidget {
  const _AppBootstrap();

  @override
  Widget build(BuildContext context) {
    return HouseKeepApp();
  }
}
```

- [ ] **Step 4: Verify Firebase initialization and preserve external blockers clearly**

Run:

```bash
flutter analyze
flutter run -d linux
```

Expected:
- Analyzer passes.
- The app starts and does not crash during Firebase initialization on supported platforms.
- Linux desktop can still launch because unsupported Firebase platforms are caught explicitly.
- If `flutterfire configure` or console credentials block this task, stop here, keep the codebase otherwise complete, and record the exact blocker in the Phase 0 checklist instead of guessing.

- [ ] **Step 5: Commit Firebase bootstrap once verified**

Run:

```bash
git add android ios lib/main.dart lib/firebase_options.dart pubspec.yaml pubspec.lock
git commit -m "feat: initialize firebase"
```

---

### Task 6: Final Verification And Documentation Sync

**Files:**
- Modify: `docs/PHASE_CHECKLIST.md`
- Modify: `README.md`

- [ ] **Step 1: Run the final verification commands in order**

Run:

```bash
flutter doctor
flutter pub get
flutter gen-l10n
dart run build_runner build --delete-conflicting-outputs
flutter analyze
flutter test test/app_smoke_test.dart
flutter run -d linux
```

Expected:
- All repo-local checks pass.
- `flutter test test/app_smoke_test.dart` passes.
- The app opens with Home, Items, Documents, and Settings tabs.

- [ ] **Step 2: Update `docs/PHASE_CHECKLIST.md` with only the items that are truly complete**

Edit the Phase 0 section so it reflects reality. Use this pattern:

```md
### Flutter SDK
- [x] Instalar Flutter SDK (stable channel)
- [x] Verificar `flutter doctor` sin errores
- [x] Configurar Android SDK (API 34+)
- [ ] Verificar Xcode + CocoaPods (si desarrollas en macOS)
```

Continue this exact approach for every Phase 0 subsection:
- mark repo-local completed tasks as `[x]`
- keep externally blocked tasks as `[ ]`
- add a short note nearby only if a blocker needs explanation

- [ ] **Step 3: Update `README.md` only if the full phase is complete**

If every required Phase 0 task is done, change the Phase 0 row to complete:

```md
| 0 | Setup Flutter, dependencias, tema, i18n, drift, Firebase | Completada |
```

If any required task is still externally blocked, keep it as pending and do not overstate completion.

- [ ] **Step 4: Commit the verification and documentation sync**

Run:

```bash
git add docs/PHASE_CHECKLIST.md README.md
git commit -m "docs: update phase 0 progress"
```

- [ ] **Step 5: Record the exact evidence in the handoff summary**

Include these concrete outputs in the final execution report:
- Flutter version used
- Whether `flutter doctor` is fully clean or clean except for non-Linux iOS tooling
- Whether `flutterfire configure` succeeded
- Whether `flutter run` launched the four-tab shell
- Which Phase 0 checklist items remain unchecked and why

---

## Spec Coverage Check

- Flutter project creation: covered by Task 1.
- Architecture-aligned folders and files: covered by Tasks 2, 3, and 4.
- Theme and app shell with `ui-ux-pro-max` guidance: covered by Task 2 and Task 3.
- i18n with English and Spanish ARB files: covered by Task 3.
- drift schema and generation: covered by Task 4.
- Firebase bootstrap and verification: covered by Task 5.
- Verification commands and checklist sync: covered by Task 6.

## Self-Review Check

- No `TODO`, `TBD`, or deferred implementation placeholders remain.
- Task order matches the approved two-block strategy from the spec.
- File paths and identifiers are consistent across tasks.
- The plan keeps visual guidance from `ui-ux-pro-max` while preserving the project palette already fixed in `docs/ARCHITECTURE.md`.
